# Timer-Clock
A simple Jetpack Compose Android app that displays the current time (with AM/PM), date, and weekday. Includes a start/stop timer feature with live updates every second, styled UI using Material3 components, and clean coroutine-based state management.
